app = 'exsize-api-prod'
primary_region = 'fra'

[build]

[env]
  # DATABASE_URL set via `fly secrets set` (Neon PostgreSQL main branch)
  CORS_ORIGINS = 'https://exsize.crstl.workers.dev'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

[mounts]
  source = 'exsize_data'
  destination = '/data'
